# Modern NCPA

A desktop application for monitoring network adapters and latency, built with **Electron** and **Python (FastAPI)**.

## 🚀 Features
- **Network Adapters:** Displays all available network adapters (Ethernet, Wi-Fi, Bluetooth, virtual adapters, etc.) with their current status (connected/disabled/identifying).
- **Latency Dashboard:** Multi-threaded ping of 20 random public IP addresses in real time.

## 📦 Installation

1. Clone the repository:
   ```bash
   git clone <your-repo-link>
   cd modern-ncpa
   ```

2. Install Python dependencies:
   ```bash
   pip install -r requirements.txt
   ```

3. Install Node.js dependencies:
   ```bash
   npm install
   ```

## ▶️ How to Run
Simply run the following command to start both Backend and Frontend:
```bash
npm start
```